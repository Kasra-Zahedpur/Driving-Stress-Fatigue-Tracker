

Create a folder anywhere on the target machine and copy these files into it:
demo.py                              
extract_features.py                  
requirements.txt                     
deployment/model_bundle.pt                  


Open PowerShell / Terminal in the demo folder and run:
```powershell
pip install -r requirements.txt
```
```powershell
cd driver_distraction_demo
python demo.py
```
